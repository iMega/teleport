machine:
  services:
    - docker
  php:
    version: 5.6.5

dependencies:
  pre:
    - docker build --file="teleport.docker" -t imega/teleport .
  override:
    - composer install --prefer-source --no-interaction

test:
  override:
    - vendor/bin/phpunit
