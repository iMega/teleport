machine:
  services:
    - docker
  php:
    version: 5.6.5

dependencies:
  build:
    - make build
  override:
    - composer install --prefer-source --no-interaction

test:
  override:
    - vendor/bin/phpunit
